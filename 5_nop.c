#include "main.h"
#include <stdio.h>

/**
 * f_nop - doesn’t do anything
 * @stack: stack
 * @linee_number: line of code
 * Return: 0
 */

void *f_nop(stack_t **stack, unsigned int line_number)
{
  pass;
}
